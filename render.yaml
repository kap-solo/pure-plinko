services:
  - type: web
    name: pure-plinko
    runtime: node
    plan: free
    buildCommand: git config --global url."https://github.com/".insteadOf "git@github.com:" && npm ci
    startCommand: npm start
    healthCheckPath: /
